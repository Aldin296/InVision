@page "/notes"
@using InVision.Data

<RadzenPanel>
    <HeaderTemplate>
        <RadzenText TextStyle="TextStyle.H6" Class="rz-display-flex rz-align-items-center rz-m-0">
            <RadzenIcon Icon="assignment" class="rz-mr-1" /><b>Notes</b>
        </RadzenText>
        <RadzenIcon Icon="add_circle_outline" @onclick="()=>OnAddItem()" Style="cursor: pointer; padding-right: 20px; font-size: 2em" class="add-todoitem-icon"></RadzenIcon>
    </HeaderTemplate>

    <ChildContent>
        <br />
        <RadzenFormField Text="" Variant="Variant.Text" Style="padding-left: 7px">
            <RadzenTextBox @bind-Value="@searchValue" @oninput="SearchNotes" Style="width: 100%;" />
        </RadzenFormField>
        <RadzenIcon style="font-weight: 20; margin-top: 20px" Icon="search"></RadzenIcon>
        <br />
        <br />


        <RadzenDataList TItem="Note" Data="noteslist" WrapItems="true" AllowPaging="true">
            <Template Context="noteslist">
                <RadzenCard Style="width: 500px">
                    <RadzenHtmlEditor @bind-Value=@htmlValue style="height: 300px;" UploadUrl="upload/image">
                        
                        <RadzenHtmlEditorUndo />
                        <RadzenHtmlEditorRedo />
                        <RadzenHtmlEditorSeparator />
                        <RadzenHtmlEditorBold />
                        <RadzenHtmlEditorItalic />
                        <RadzenHtmlEditorUnderline />
                        <RadzenHtmlEditorStrikeThrough />
                        <RadzenHtmlEditorAlignLeft />
                        <RadzenHtmlEditorAlignCenter />
                        <RadzenHtmlEditorAlignRight />
                        <RadzenHtmlEditorJustify />
                        <RadzenHtmlEditorIndent />
                        <RadzenHtmlEditorOutdent />
                        <RadzenHtmlEditorUnorderedList />
                        <RadzenHtmlEditorOrderedList />
                        <RadzenHtmlEditorColor />
                        <RadzenHtmlEditorBackground />
                        <RadzenHtmlEditorRemoveFormat />
                        <RadzenHtmlEditorLink />
                        <RadzenHtmlEditorUnlink />
                        <RadzenHtmlEditorImage />
                        <RadzenHtmlEditorFontName />
                        <RadzenHtmlEditorFontSize />
                    </RadzenHtmlEditor>

                </RadzenCard>
            </Template>

        </RadzenDataList>


    </ChildContent>
</RadzenPanel>



@code {
    string searchValue;
    List<Note> noteslist = new List<Note>
    {
        new Note(){ },
        new Note(){ },
        new Note(){ },
        new Note(){ },
        new Note(){ },
    };

    string htmlValue;

    protected override async Task OnInitializedAsync()
    {

    }

    private async void OnAddItem()
    {

    }

    private void SearchNotes(ChangeEventArgs args)
    {

    }
}
